import { Component } from '@angular/core';
import { Header } from '../../components/header/header';

@Component({
  selector: 'app-feed',
  imports: [Header],
  templateUrl: './feed.html',
  styleUrl: './feed.css',
})
export class Feed {}
